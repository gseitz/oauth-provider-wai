language: haskell
before_install:
        - git clone https://github.com/gseitz/oauth-provider.git
        - cabal sandbox init
        - cabal sandbox add-source oauth-provider
script:
        - cabal configure --enable-tests
        - cabal build
        - cabal test --show-details=always

